
functions:

  can be treated as objects
  function can be passed as argument to other functions
  function can be returned by other function
  
//fat arrow functions -- similar to lambda in java

   function display(fn1){
	  var result= fn1(45,67);
	  return result
   }
   
   result= display((x,y)=>x-y)  // same as result = display(function(a,b){return a+b})
 
   console.log(result)
   
   //functuion returning another function:
   
   function abc(){
	   
	   function hello(a,b){
		   return a / b;
	   }
	   return hello;
   }
 
  fn = abc();
  
   console.log(fn(120,11))
